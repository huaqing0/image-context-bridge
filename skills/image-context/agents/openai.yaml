interface:
  display_name: "Image Context Bridge"
  short_description: "Converts images into OCR-based text context for text-only models."
  default_prompt: "Use image-context to convert the provided image into a text evidence packet before answering."
policy:
  allow_implicit_invocation: true
